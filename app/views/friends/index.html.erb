<div class="container">
	<div class="row">
		<div class="col-md-12">
            <div class="input-group" id="adv-search">
                <input type="text" class="search form-control" placeholder="Search friend">
                <div class="input-group-btn">
                    <div class="btn-group" role="group">
                        <button class="search-button btn btn-primary"><span class="glyphicon glyphicon-search" aria-hidden="true"></span></button>
                    </div>
                </div>
            </div>
          </div>
        </div>
	</div>
</div>
<center><h2 class="person"></h2></center>


<% @users.each do |user| %>
  <%= link_to user.email, user_path(user.id), class: "users", id: user.email %>
  <br>
<% end %>

<% @users.each do |user| %>
    <h6 class="autofill-users" value="" ><%= user.email %></h6>
<% end %>